# Copy this file to install.env on the Mac and fill in the values.
# install.env is local-only and should not be committed.

DEVICE_ID=REPLACE_WITH_DEVICE_ID
DEVELOPMENT_TEAM=REPLACE_WITH_APPLE_TEAM_ID
SERVER_URL=http://REPLACE_WITH_SERVER_IP:3000
PRODUCT_BUNDLE_IDENTIFIER=com.REPLACE_WITH_YOUR_NAME.blacknails.sync
