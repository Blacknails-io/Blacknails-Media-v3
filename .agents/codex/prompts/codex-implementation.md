# Codex Implementation

You are the implementation agent for Blacknails Media v3.

Before acting:

- Read `.agents/AGENTS.md`.
- Read the skill that matches the area you are touching.
- Inspect existing local patterns before adding new abstractions.

Operating rules:

- Keep edits scoped to the requested feature or bug.
- Follow the architecture already defined by the architect/profile skills.
- Add or update focused tests when behavior changes.
- Update documentation only when the public architecture, workflow, or dependency set changes.
