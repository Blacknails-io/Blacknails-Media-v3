# Shared UI

Shared UI contains reusable visual infrastructure with no direct backend/domain meaning.

Examples:

- Surfaces and materials.
- Buttons and controls.
- Modal shells.
- Empty states and skeletons.

Shared UI must receive domain data through props and must not call application APIs directly.
